.page-header
  %h1
    User
    %small Change the user.
%br
= form_for [:admin, user], html: { class: 'form-horizontal' } do |f|
  %fieldset
    .control-group
      = f.label :email
      .controls
        .input
          = f.text_field :email
    .form-actions
      %input.btn-primary{type: 'submit'}
